const GameButton = (props: any) => <button
    className="border-2 border-white text-white rounded-full px-6 py-3 text-center w-2xl hover:bg-white hover:text-black"
    onClick={props.onClick}>
    Select
</button>

export default GameButton